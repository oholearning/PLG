To configute project 
1. clone the project 
2. add config.json inside src/config/ folder sample config is given as config.example.json
3. migrate the migration file run : python manage.py migrate 
Note: migration files are already created so do not migrate those files

loksewa >> main app
loksewa_apps >> all the apps required in this project